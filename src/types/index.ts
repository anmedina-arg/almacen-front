/**
 * Re-export all catalog types for backward compatibility.
 * The canonical source of truth is src/features/catalog/types/.
 * New code should import from '@/features/catalog/types' directly.
 */
export * from '@/features/catalog/types';
